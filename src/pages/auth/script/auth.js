import UserCredential from '@/components/user-credential'
import UserInfo from '@/components/user-info'

export default {
    data () {
        return {
        }
    },

    mounted() {
    },

    components: {
        UserCredential,
        UserInfo
    }
}